<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.luotuoyulang.redis.mapper.DogMapper">

    <!--&lt;!&ndash; 查询所有用户 &ndash;&gt;-->
    <!--<select id="getUsers" parameterType="User" resultType="User">-->
		<!--select * from `user` -->
	<!--</select>-->

    <select id="findDog" resultType="cn.luotuoyulang.redis.entity.Dog">
        select * from Dog;
    </select>
</mapper>
